import SwiftUI

struct AdminMyPageView: View {
    var body: some View {
        VStack {
            Text("AdminMyPage Screen")
                .font(.largeTitle)
                .padding()
        }
    }
}
