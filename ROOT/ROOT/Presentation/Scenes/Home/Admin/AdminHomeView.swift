import SwiftUI

struct AdminHomeView: View {
    var body: some View {
        VStack {
            Text("AdminHome Screen")
                .font(.largeTitle)
                .padding()
        }
    }
}
