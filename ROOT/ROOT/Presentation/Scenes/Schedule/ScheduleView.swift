import SwiftUI

struct ScheduleView: View {
    var body: some View {
        VStack {
            Text("Schedule Screen")
                .font(.largeTitle)
                .padding()
        }
    }
}
